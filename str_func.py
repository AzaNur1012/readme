def str_func():
    """функция перевода всех букв в верхний регистр"""
    a = input() #ввод строки
    return (print(a.upper())) #возврат переделанной строки

def str_title():
    """функция перевода заглавных букв в верхний регистр"""
    a = input() #ввод строки
    return (print(a.title())) #возврат переделанной строки